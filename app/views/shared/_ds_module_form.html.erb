
<%= form_for @ds_module, :html => { :multipart => true } do |f| %>
	<%= render 'shared/error_messages', :object => f.object %>
	<div class="field">
		<%= f.label :name %>
		<%= f.text_field :name %>
	</div>
	<div class="field">
		<%= f.label :version %>
		<%= f.text_field :version %>
	</div>
	<div class="field">
		<%= f.label :description %><br />
		<%= f.text_area :documentation %>
	</div>
	<% if @ds_module.get_images.count > 0 %>	
	    <p><em>Attached images:</em></p>
        <%= coderay(@ds_module.get_images_markdown).html_safe %>
    <% end %>	
	<div class="field">
		<%= f.label :example %><br />
		<%= f.text_area :example %>
	</div>
	<div class="field">
		<%= f.label "Category[ies]" %>
		<%= f.text_field :category %>
	</div>
	<p><em>Separate with commas (e.g. Example, Library, Architecture, Engineering, Environmental)</em></p> 
	<div class="field">
		<%= f.label :DesignScript_file_or_zip %>
		<%= f.file_field :ds_attachment %>
	</div>
	<div class="actions">
		<%= f.submit "Submit" %>
	</div>
<% end %>
